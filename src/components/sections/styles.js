'use strict';

import settings from '../../styles/settings';

const section = {
};

const sectionHeader = {
  marginBottom: 30
};

const sectionTitle = {
  color: settings.primaryColor,
  fontSize: 24,
  fontWeight: 300,
  textAlign: 'center'
};

const sectionText = {
  textAlign: 'center'
};

const sectionFooter = {
  marginTop: 30
};

export default {
  section,
  sectionHeader,
  sectionTitle,
  sectionText,
  sectionFooter
};

